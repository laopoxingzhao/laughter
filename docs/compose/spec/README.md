# Compose 设计文档

本目录存放 **Compose Next** 工作流的功能设计/交付记录。

**现行语言语义以 [`../../LANGUAGE.md`](../../LANGUAGE.md) 与当前代码为准。**

| 文档 | 说明 |
|------|------|
| [lang-rewrite.md](lang-rewrite.md) | 分层包彻底重写（契约 v2）的交付记录；特性汇总亦见 LANGUAGE.md |

更早各期（MVP、MVP++、lang-next、ZCA 等）的 spec 已删除，避免与 v2 契约冲突；历史可在 `git log` / `git show` 中查阅。

新功能若走 Compose：在本目录新增 `docs/compose/spec/<feature>.md`，交付后更新本索引。
