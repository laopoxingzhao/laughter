[package]
name = "laughter"
version = "0.2.0"
edition = "2021"
description = "Teaching language: lexer → parser → typecheck → bytecode → VM"
license = "MIT"

[lib]
name = "laughter"
path = "src/lib.rs"

[[bin]]
name = "laughter"
path = "src/bin/laughter.rs"

[dependencies]
zip = "2"
