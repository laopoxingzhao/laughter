[package]
name = "laughter"
version = "0.1.0"
edition = "2021"
description = "A small teaching language: lexer → parser → typecheck → bytecode → VM"
license = "MIT"

[lib]
name = "laughter"
path = "src/lib.rs"

[[bin]]
name = "laughter"
path = "src/bin/laughter.rs"

[dependencies]

[profile.release]
lto = true
